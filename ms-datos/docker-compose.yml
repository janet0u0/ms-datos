services:
  mysql-datos:
    image: mysql:8.0
    container_name: mysql-ms-datos
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: ms_datos_db
    ports:
      - "3308:3306"
    volumes:
      - mysql_datos_data:/var/lib/mysql

volumes:
  mysql_datos_data: